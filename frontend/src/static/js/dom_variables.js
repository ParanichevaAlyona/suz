const BACKEND_URL = document.body.dataset.backendUrl;
const handlerSelect = document.getElementById('handler-select');
const executeBtn = document.querySelector('.enqueue-btn');
const statusDiv = document.getElementById('status');

const textarea = document.getElementById("inputParam");
const emptyState = document.getElementById('emptyState');

const sidebar = document.getElementById('sidebar');
const sidebarContent = document.getElementById('sidebar-content');
const baseContainer = document.getElementById('base-container');
